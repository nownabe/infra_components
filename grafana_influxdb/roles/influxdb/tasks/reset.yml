- service:
    name: influxdb
    state: stopped
    enabled: false

- yum:
    name: influxdb
    state: absent

- file:
    path: "{{ item }}"
    state: absent
  with_items:
    - /var/lib/influxdb
    - /etc/default/influxdb
    - /etc/influxdb
